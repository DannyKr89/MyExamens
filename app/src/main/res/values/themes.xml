<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.MyExamens" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/background_color</item>
    </style>

    <style name="Theme.MyExamens" parent="Base.Theme.MyExamens" />

    <style name="BottomCustomIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">#223637</item>
        <item name="android:width">100dp</item>
        <item name="android:height">60dp</item>
        <item name="cornerSize">16dp</item>
        <item name="shapeAppearance">@style/CustomShapeAppearance</item>
    </style>

    <style name="CustomShapeAppearance" parent="ShapeAppearance.Material3.NavigationBarView.ActiveIndicator">
        <item name="cornerSize">30%</item>
    </style>
</resources>